@model IEnumerable<WebApplication3.Models.Stadium>

<p>
    <a class="btn btn-info" asp-action="Create">Create New</a>
</p>
<table class="table table-hover" >
    <thead>
        <tr>
          
            <th>
                @Html.DisplayNameFor(model => model.Std_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Std_CityName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Std_Capacity)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           
            <td>
              @item.Std_Name
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Std_CityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Std_Capacity)
            </td>
            <td>
                <a  class="btn btn-success"  asp-action="Edit" asp-controller="Stadium" asp-route-id="@item.Id">Edit</a>
                <a  class="btn btn-danger"  asp-action="Delete" asp-controller="Stadium" asp-route-id="@item.Id">Delete</a>
              
              @*  @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.PrimaryKey  })*@
            </td>
        </tr>
}
    </tbody>
</table>
